import * as Vector3 from './Vector3';
import * as mathUtils from './mathUtils';

// Bug: api-extractor@5.10.3 create an ASTNamespace following export statement, but with incorrect name as the file path
export { Vector3, mathUtils };
